define('ivy/CallableObject', [], function() {
	function CallableObject(name, codeObj, dirInterp) {
		this._name = instrs;
		this._codeObj = codeObj;
		this._dirInterp = dirInterp;
	};
	return __mixinProto(CallableObject, {
		
	});
});